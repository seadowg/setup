1. Sign into 1Password and enable SSH Agent
2. Add PGP Key to GPG Keychain and set as Default Key in GPG Suite settings
4. Set Mountain Duck to sync to `~/Mounts`
3. Use checkin to pull in `notes` project to `~/workspace/Notes`
3. Setup Obsidian to sync vault to `~/workspace/Notes`
5. Setup LanguageTool by running: `brew services start languagetool`
6. Setup `rclone` with `dropbox` remote by running `rclone config`
